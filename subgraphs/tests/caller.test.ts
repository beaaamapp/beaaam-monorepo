import {
  assert,
  describe,
  test,
  clearStore,
  beforeAll,
  afterAll
} from "matchstick-as/assembly/index"
import { Address, BigInt } from "@graphprotocol/graph-ts"
import { Authorized } from "../generated/schema"
import { Authorized as AuthorizedEvent } from "../generated/Caller/Caller"
import { handleAuthorized } from "../src/caller"
import { createAuthorizedEvent } from "./caller-utils"

// Tests structure (matchstick-as >=0.5.0)
// https://thegraph.com/docs/en/developer/matchstick/#tests-structure-0-5-0

describe("Describe entity assertions", () => {
  beforeAll(() => {
    let sender = Address.fromString(
      "0x0000000000000000000000000000000000000001"
    )
    let authorized = Address.fromString(
      "0x0000000000000000000000000000000000000001"
    )
    let newAuthorizedEvent = createAuthorizedEvent(sender, authorized)
    handleAuthorized(newAuthorizedEvent)
  })

  afterAll(() => {
    clearStore()
  })

  // For more test scenarios, see:
  // https://thegraph.com/docs/en/developer/matchstick/#write-a-unit-test

  test("Authorized created and stored", () => {
    assert.entityCount("Authorized", 1)

    // 0xa16081f360e3847006db660bae1c6d1b2e17ec2a is the default address used in newMockEvent() function
    assert.fieldEquals(
      "Authorized",
      "0xa16081f360e3847006db660bae1c6d1b2e17ec2a-1",
      "sender",
      "0x0000000000000000000000000000000000000001"
    )
    assert.fieldEquals(
      "Authorized",
      "0xa16081f360e3847006db660bae1c6d1b2e17ec2a-1",
      "authorized",
      "0x0000000000000000000000000000000000000001"
    )

    // More assert options:
    // https://thegraph.com/docs/en/developer/matchstick/#asserts
  })
})
